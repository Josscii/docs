# Aisten Docs

Aisten 的文档站，现已迁移到 Fumadocs + Next.js。

## 开发

```bash
npm install
npm run dev
```

默认开发地址为 `http://localhost:3000`。

## 内容目录

- `content/docs`：文档内容
- `app`：Next.js 路由与布局
- `components/docs`：品牌组件与迁移兼容组件
